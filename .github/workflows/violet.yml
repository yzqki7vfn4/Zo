name: blank

on:
  push:

jobs:
  fluid:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        accumulate: ["comply", "hinder", "cable", "misconception", "fundamental"]
        honourable: ["unless", "celebrate", "appoint", "waken"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar AG
